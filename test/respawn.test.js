var helper = require('./support/helper')
  , Qilin = require(global.test.root + '/lib/qilin');

describe('Qilin', function() {
  var qilin;

  beforeEach(function(done) {
    //qilin = new Qilin(
    //    {
    //    }
    //  , {
    //    }
    //);
  });

  afterEach(function(done) {
  });

  it('respawns killed process');
});
